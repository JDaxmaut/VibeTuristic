services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: "vibe-turistic-temp-key-change-before-real-prod-2025"
      ALLOWED_HOSTS: "*"
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
